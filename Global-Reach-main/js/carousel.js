/**
 * STEP-1 BOUNDARY
 * This file acts as the shared carousel engine only.
 * Do NOT add carousel-specific behaviour here.
 * Per-carousel behaviour must be declarative config (Step 2).
 */

// Shared carousel engine placeholder (Step 2)